import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.torino.scanner',
  appName: 'TorinoStorageSolutions',
  webDir: '.output/public'
};

export default config;
